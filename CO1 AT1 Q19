def recurrence(n):
    if n == 1:
        return 1
    return recurrence(n // 2) + n
n = int(input("Enter input size (power of 2): "))
print("Result =", recurrence(n))
print("Time Complexity = Θ(n)")
